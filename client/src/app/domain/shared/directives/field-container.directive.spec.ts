import { FieldContainerDirective } from './field-container.directive';

describe('FieldContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldContainerDirective();
    expect(directive).toBeTruthy();
  });
});
